# Web

React + Vite + TypeScript app seed with Vitest + Testing Library configured, so you
can develop test-first from the start.

- `npm run dev` — dev server
- `npm test` — run tests
- `npm run typecheck` — type check
- `npm run build` — production build

Add components under `src/`; keep each with a matching `*.test.tsx`.
